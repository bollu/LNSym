import Arm.Cfg.Cfg

/-
- Create a correspondence between (BitVec 5) and Nat for the index set.
- Question: how do we map the semantics that's written in terms of the old memory model?
- Question: I guess we have a mapping from virtual to real registers, and we show that
    the virtual register matches the real register values.
-/

